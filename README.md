# GodKey Skill

> 通用结构化编程方法论 —— 不跳到结论、不走捷径、不留垃圾代码。

---

## 这是什么

GodKey Skill 是一个 Claude Code Skill，它替换 AI 的默认行为模式：

```
原来的 AI：快速找到相关代码 → 最小改动 → 完成
加载后：  建立判断标准 → 理解完整关系 → 确定改动边界 → 精确改动 → 自检循环 → 文档同步
```

它不是给你一堆规则条目让你参考，而是**锁定流程、聚焦注意力**——在关键节点强制 AI 停下来、输出结果，不输出就不能继续。

---

## 核心设计

| 组件 | 角色 |
|------|------|
| **SKILL.md** | 编排器：5 关 + 12 禁 + 阻塞铁律 + 场景调度 |
| **开发流程** | 从读规范 → 识框架 → 追依赖 → 定层 → 执行自检 的完整链路 |
| **代码审查** | 5 维度原则驱动式审查（非固定清单） |
| **测试流程** | 真实链路验证纪律 |
| **文档同步** | 代码落盘后的文档动作规则 |
| **项目模板** | 完整 10 目录文档体系模板，新项目一键初始化 |

---

## 安装

```bash
git clone https://github.com/godkey1314/godkey-skill.git ~/.claude/skills/godkey-skill
```

安装后 Claude Code 自动识别。在代码开发、审查、重构时自动触发，也可手动触发：

> 使用 godkey-skill，帮我审查代码

---

## 适用场景

- **代码开发**：新功能、改 Bug、加模块
- **代码审查**：PR Review、工作区审查
- **代码重构**：跨模块重构、架构调整
- **项目初始化**：自动搭建文档体系

不适用于：单行修改、纯问答、格式微调、文案修改。

---

## 结构

```
godkey-skill/
├── SKILL.md                      # 编排器：关卡 + 禁令 + 阻塞铁律 + 场景调度
├── references/
│   ├── 开发流程.md                # 开发子流程：读规范 → 识框架 → 追依赖 → 定层 → 自检
│   ├── 代码审查.md                # 审查子流程：5 维度原则驱动式审查
│   ├── 测试流程.md                # 测试子流程：真实链路验证
│   └── 文档同步.md                # 文档子流程：日志、计划、设计文档同步规则
└── assets/
    └── project-template/          # 完整项目文档模板（10 目录体系）
```

---

## 设计理念

**Skill 不是知识库，是调度器。** 它不教 AI 编码规范——规范在项目自己的 `doc/` 里。Skill 只做一件事：在正确的时间，把 AI 的注意力强制指向正确的位置。

- 不让 AI "参考一下规则" → 规定 AI "从项目规范提取约束，逐条对照自检"
- 不让 AI "自己判断读够了" → 要求 AI "输出文件清单证明读完了"
- 不让 AI "改完就完了" → 强制 AI "改完自检、循环修复、文档同步"
- 不预设"检查并发、检查前端" → 审查标准来自项目规范，Skill 只提供审查维度框架

---

## License

MIT
